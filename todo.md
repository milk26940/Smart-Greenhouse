# 智慧农业大棚 Todo

## 已完成

- [x] MX6U 最小工程骨架已建立，支持 `Makefile` 构建
- [x] 虚拟机交叉编译与 NFS 挂载部署链路已打通
- [x] 开发板已可从 `/mnt/nfs/smart-greenhouse/mx6u` 运行程序
- [x] MX6U 已接入 `/dev/ttymxc2` 串口收包循环
- [x] `ENV/STAT` 上行协议解析代码已完成
- [x] `ENV` 帧已在开发板实机完成收包、解析和打印验证
- [x] 已明确首版 MQTT 采用 `MX6U -> broker.emqx.io:1883`

## 当前待做

- [x] 在 MX6U 侧实现状态缓存
- [x] 将最近一次 `ENV` 与最近一次 `STAT` 关联成当前大棚状态
- [ ] 完成 `STAT` 帧的开发板实机联调与验证
- [x] 增加节点最近上报时间记录
- [x] 实现节点离线检测逻辑
- [x] 为后续本地规则控制预留清晰的数据接口
- [x] 补充状态缓存与状态汇总的测试用例
- [x] 将最新开发、联调结论持续同步到相关文档

## STM32 侧待做

> 当前先不处理，保留为后续 STM32 专项任务。

- [ ] 接入独立土壤湿度传感器，替换当前 `SOIL` 预留/占位状态
- [ ] 确认执行节点 `STAT` 上报在实物链路中稳定可见
- [ ] 按双角色约定继续维护采集节点与执行节点配置

## 后续扩展

- [x] 实现本地规则判断与自动控制闭环
- [x] 增加温度控制、灌溉控制、补光控制规则
- [x] 增加异常检测与设备状态联动判断
- [x] 增加趋势预测、提前干预、多源融合能力
- [x] 评估并实现 OpenCV 简单视觉能力
- [ ] 完成 MQTT 发布链路交叉编译、开发板运行与 broker 订阅联调
- [ ] 规划并实现本仓库网页对 MQTT WebSocket 的订阅展示

## 备注

- 当前主线目标仍是先完成 `LoRa 上行 -> MX6U 接收解析 -> 本地状态形成 -> 本地验证`
- 当前阶段先完成 `MX6U -> MQTT broker` 发布链路，再补网页展示
- 当前 MQTT 默认目标已切换为 `broker.emqx.io:1883`，优先保证首版上云链路可实机跑通
- 当前实物链路已确认在线的只有采集节点 `ENV node=1`
- 其余采集节点 `2/3` 和执行节点 `4/5` 当前仍按预留槽位处理，未完成实机联通验证
- 当前自动控制闭环仍停留在 MX6U 本地决策层，尚未对 STM32 下发真实控制命令
- 当前 OpenCV 视觉模块已接入主程序，开发板 `/dev/video2` 已实测抓拍成功，当前可稳定输出 `VISION SNAPSHOT available=YES`
