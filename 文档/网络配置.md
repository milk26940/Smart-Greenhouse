192.168.137.1 PC
192.168.137.3 虚拟机 lan Abc123456。
192.168.137.106 开发板 ssh -o HostKeyAlgorithms=+ssh-rsa -o PubkeyAcceptedAlgorithms=+ssh-rsa root@192.168.137.106

开发板当前已配置为固定 IP：

- 接口：`eth1`
- 地址：`192.168.137.106`
- 子网掩码：`255.255.255.0`
- 网关：`192.168.137.1`

开发板持久化网络配置文件：

- `/etc/network/interfaces`

本次在开发板上执行的命令：

- 备份：`cp /etc/network/interfaces /etc/network/interfaces.bak-20260319`
- 配置修改：最终确认实际联网口是 `eth1`，将 `eth1` 设为 `static 192.168.137.106`，`eth0` 保持 `dhcp`
- 生效验证：`/etc/init.d/networking restart`

## NFS 挂载

- 当前已打通的 NFS 链路为：虚拟机 `192.168.137.3` -> 开发板 `192.168.137.106`。
- 虚拟机实际导出目录为 `/home/lan/linux/nfs`，不是 `/home/lan/linux`。
- 开发板挂载点为 `/mnt/nfs`。
- 开发板验证通过的挂载命令为 `mount -t nfs -o vers=3,nolock,proto=tcp 192.168.137.3:/home/lan/linux/nfs /mnt/nfs`。
- 开发板 `/etc/fstab` 已写入 `192.168.137.3:/home/lan/linux/nfs /mnt/nfs nfs vers=3,nolock,proto=tcp 0 0`，可通过 `mount -a` 重新挂载验证。
- 需要共享文件给开发板时，优先放入虚拟机 `/home/lan/linux/nfs`，再从开发板 `/mnt/nfs` 访问。

## 当前开发约定

- MX6U 侧代码与构建统一放在虚拟机 `/home/lan/linux/nfs` 下，不再以桌面目录作为实际构建位置。
- 交叉编译在虚拟机内完成，开发板只负责通过 `/mnt/nfs` 访问和运行构建产物。
- 当前首版 MQTT 发布目标为 `broker.emqx.io:1883`，网页端后续使用 `wss://broker.emqx.io:8084/mqtt` 订阅同一主题。
- 当前实测结果：同一最小 MQTT 客户端对 `broker.emqx.io:1883` 可收到标准 `CONNACK`，优先将其作为当前项目的默认公网 broker。

## 本次连接经验

- 开发板最新 IP 已变更为 `192.168.137.106`，原 `192.168.137.155` 现在只应视为旧地址记录。
- Windows 主机首次连接 `192.168.137.106` 时会出现 host key 确认提示，因为该开发板之前曾以 `192.168.137.155` 写入 `known_hosts`。
- 本次在主机上确认通过的登录命令为：

```bash
ssh -o HostKeyAlgorithms=+ssh-rsa -o PubkeyAcceptedAlgorithms=+ssh-rsa root@192.168.137.106
```

- 首次连接时出现的典型提示为：
  - `The authenticity of host '192.168.137.106' can't be established.`
  - `This host key is known by the following other names/addresses: ... 192.168.137.155`
- 遇到上述提示时，确认当前 IP 无误后输入 `yes` 即可接受新的 host key。
- 如需清理旧地址残留记录，可在 Windows 主机执行：

```powershell
ssh-keygen -R 192.168.137.155
```

- 该 IMX6U 系统未安装 `sshd`，实际使用的是 `dropbear`：
  - `which sshd` 无结果
  - `which dropbear` 返回 `/usr/sbin/dropbear`
- `/etc/init.d/dropbear start` 已验证可用；`netstat -tnlp | grep :22` 可看到 `575/dropbear` 在 `0.0.0.0:22` 和 `:::22` 监听。
- 若后续再次出现“主机能 ping 通但外部 SSH 连不上”，优先在开发板本机检查：

```bash
/etc/init.d/dropbear start
netstat -tnlp | grep :22
```
