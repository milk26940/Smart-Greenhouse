# 虚拟机与开发板记录

维护约定：

- 所有与虚拟机和开发板相关的操作，视情况持续补充到本文档
- 重点记录地址、登录方式、目录位置、设备验证结果和关键配置调整

## 1. 虚拟机

- 虚拟机地址：`192.168.137.3`
- 虚拟机用户：`lan`
- 虚拟机桌面真实路径：`/home/lan/桌面`

### 1.1 项目目录

本次已在虚拟机桌面创建项目目录：

- 正确目录：`/home/lan/桌面/智慧农业大棚`
- 当前状态：空目录，尚未创建子目录和工程文件

同时确认到系统中还存在一个英文桌面目录：

- 额外目录：`/home/lan/Desktop`

之前误在该目录下创建过：

- `/home/lan/Desktop/智慧农业大棚`

当前误建目录状态：

- `/home/lan/Desktop/智慧农业大棚` 也为空目录

当前按用户实际桌面路径使用 `~/桌面`，不要把 `/home/lan/Desktop` 误认为系统桌面。

### 1.2 当前桌面结构

当前虚拟机桌面 `/home/lan/桌面` 下可见：

- `新建文件夹/`
- `智慧农业大棚/`
- `exp/`
- `fsl-imx-x11-glibc-x86_64-meta-toolchain-qt5-cortexa7hf-neon-toolchain-4.1.15-2.1.0_20241230.sh`

## 2. 开发板

- 开发板当前地址：`192.168.137.71`
- 登录用户：`root`
- SSH 登录命令：

```bash
ssh -o HostKeyAlgorithms=+ssh-rsa -o PubkeyAcceptedAlgorithms=+ssh-rsa root@192.168.137.71
```

### 2.1 网络配置

开发板当前已配置为固定 IP：

- 接口：`eth1`
- 地址：`192.168.137.71`
- 子网掩码：`255.255.255.0`
- 网关：`192.168.137.1`

开发板持久化网络配置文件：

- `/etc/network/interfaces`

本次已在开发板上备份原配置：

- `/etc/network/interfaces.bak-20260319`
- `/etc/network/interfaces.bak-20260319-eth1`

### 2.2 串口通信

开发板利用串口3与 LoRa 模块通信（不需要对 LoRa 模块进行操作，直接用）。

## 3. 说明

- 文档中旧的开发板地址 `192.168.137.216` 已失效，当前实际使用 `192.168.137.71`
- 虚拟机桌面请统一使用 `~/桌面`
- 需要给开发板共享文件时，仍优先使用 NFS 导出目录 `/home/lan/linux/nfs`
- 开发板当前实际联网口是 `eth1`，不是 `eth0`
